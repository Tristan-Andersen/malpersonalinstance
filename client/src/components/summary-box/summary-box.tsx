import React from 'react';

export interface SummaryBarProps {
    name?: string;
    linkToImage?: string;
    description?: string;
    releaseDate?: string;
}

const SummaryBar = () => {
    return (
        <div>

        </div>
    );
}

export default SummaryBar;